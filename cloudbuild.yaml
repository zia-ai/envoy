steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', 
        '-t', 'gcr.io/$PROJECT_ID/$REPO_NAME:$SHORT_SHA', 
        '-f', 'docker-zia/Dockerfile',
        '.']
images:
- 'gcr.io/$PROJECT_ID/$REPO_NAME:$SHORT_SHA'
# options:
#   machineType: 'N1_HIGHCPU_64'